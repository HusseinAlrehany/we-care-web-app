export interface ClinicDTO {
    id?: number,
    clinicLocation?: string,
    clinicMobile: string,
    address: string,
    doctorId?: number,
    cityId?: number,
    stateId?: number,
    doctorName: string,
    cityName: string,
    stateName: string
}
