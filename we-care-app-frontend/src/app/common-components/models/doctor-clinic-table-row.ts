//this interface to combine data from two objects or list object in one table
export interface DoctorClinicTableRow {
    fullName: string,
    doctorEmail: string,
    specialityName: string,
    clinicMobile: string,
    fullAddress: string
}
